<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MxZ - Translator</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #000000; /* أسود غامق جدا */
      color: #ffffff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    header {
      font-size: 2.5em;
      margin-bottom: 10px;
      color: #1E90FF; /* الأزرق */
      text-align: center;
    }

    #datetime {
      font-size: 1em;
      margin-bottom: 30px;
      color: #aaa;
    }

    .translator-box {
      display: flex;
      flex-direction: column;
      gap: 15px;
      width: 90%;
      max-width: 500px;
    }

    input, select, button {
      padding: 12px;
      font-size: 1em;
      border: none;
      border-radius: 10px;
      transition: all 0.3s;
    }

    input {
      width: 100%;
      border: 1px solid #1E90FF;
      background-color: #111111; /* أسود داكن للحقول */
      color: #fff;
    }

    input:focus {
      outline: 2px solid #1E90FF;
    }

    select {
      width: 100%;
      background-color: #111111; /* أسود داكن */
      color: #fff;
      border: 1px solid #1E90FF;
      cursor: pointer;
    }

    select:focus {
      outline: 2px solid #1E90FF;
    }

    button {
      background-color: #1E90FF;
      color: #fff;
      cursor: pointer;
      font-weight: bold;
    }

    button:hover {
      background-color: #0d5bb5;
      transform: scale(1.05);
    }

    .result {
      margin-top: 10px;
      padding: 12px;
      background-color: #111111; /* أسود داكن */
      border-radius: 10px;
      min-height: 60px;
      font-size: 1.1em;
    }
  </style>
</head>
<body>
  <header>MxZ Translator</header>
  <div id="datetime"></div>
  <div class="translator-box">
    <input type="text" id="textInput" placeholder="اكتب الكلمة هنا...">
    <select id="languageSelect">
      <option value="en">الإنجليزية</option>
      <option value="ar">العربية</option>
      <option value="es">الإسبانية</option>
      <option value="fr">الفرنسية</option>
      <option value="de">الألمانية</option>
    </select>
    <button onclick="translateText()">ترجم</button>
    <div class="result" id="result">الترجمة تظهر هنا</div>
  </div>

  <script>
    // تحديث الوقت والتاريخ
    function updateDateTime() {
      const now = new Date();
      const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit', second: '2-digit' };
      document.getElementById('datetime').textContent = now.toLocaleDateString('ar-EG', options);
    }
    setInterval(updateDateTime, 1000);
    updateDateTime();

    // الترجمة
    async function translateText() {
      const text = document.getElementById('textInput').value;
      const lang = document.getElementById('languageSelect').value;
      const resultDiv = document.getElementById('result');

      if (!text) {
        resultDiv.textContent = "اكتب كلمة لترجمتها";
        return;
      }

      try {
        const response = await fetch('https://libretranslate.de/translate', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            q: text,
            source: 'auto',
            target: lang,
            format: 'text'
          })
        });
        const data = await response.json();
        resultDiv.textContent = data.translatedText;
      } catch (err) {
        resultDiv.textContent = "حدث خطأ، حاول مرة أخرى";
        console.error(err);
      }
    }
  </script>
</body>
</html>
